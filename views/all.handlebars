<h2>Todas as Tarefas:</h2>
{{#if tasks.length}}
  <ul>
    {{#each tasks}}
      <li>
        <b>{{this.title}}</b> - {{this.description}} - 
        {{#if this.done}}
            ✅ Concluída
        {{else}}
            🕗 Pendente
        {{/if}}
        <a href="/tasks/edit/{{this.id}}">Editar</a>
        <form style="display:inline;" method="post" action="/tasks/delete">
          <input type="hidden" name="id" value="{{this.id}}"/>
          <button type="submit">Excluir</button>
        </form>
        <p style="display:inline;">Prioridade: 
            {{#if (eq this.priority 0)}}Baixa{{/if}}
            {{#if (eq this.priority 1)}}Média{{/if}}
            {{#if (eq this.priority 2)}}Alta{{/if}}
        </p>
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>🎉 Nenhuma tarefa cadastrada. Que tal criar uma nova?</p>
{{/if}}